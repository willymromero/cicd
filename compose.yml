services:
  app:
    build:
      context: .
      dockerfile: dockerfile
      target: ${STAGE}
    container_name: ${CONTAINER_NAME}
    volumes:
      - .:/app/
      - /app/node_modules
    ports:
      - ${PORT}:${PORT}
    env_file:
      - .env
    restart: on-failure
